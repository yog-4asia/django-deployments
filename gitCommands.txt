git init
git clone
git status  ##git status in your working directory. lists out all the files that have been changed.
git add   ##adds changes to stage/index in your working directory.
git add -p
git commit -m ”sweet little commit message”

git branch -a  ## Utility : Lists out all the branches.

git remote add  origin <:remote_url:>
git push -u origin master

##When you want to just pull stuff somewhere
git pull <https://...> master
git pull origin master --allow-unrelated-histories

--------------------------------------------------

Remove everything from a repository:


git checkout master

git rm -r <folder-name or .>

git commit -m "Remove duplicated directory"

git push origin master

--------------------------------------------------
## To delete a branch
git push origin --delete secondary